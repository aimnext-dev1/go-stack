name: go-stack
arch: amd64
platform: linux
version: ${VERSION}
maintainer: aimnext-dev1
description: CLI to deploy, manage, backup, and restore docker compose stacks
homepage: https://github.com/aimnext-dev1/go-stack

contents:
  - src: ./go-stack
    dst: /usr/local/bin/go-stack
